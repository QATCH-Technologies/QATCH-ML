"""Namespace package for QATCH-ML's concrete pipelines.

Each subpackage here (e.g. :mod:`src.systems.qmodel_7_onyx`) implements a
self-contained, versioned sensor-run analysis pipeline with its own corpus
handling, training, and inference stages; this package itself holds no
shared logic.
"""
